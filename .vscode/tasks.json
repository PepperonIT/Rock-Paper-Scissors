{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Launch",
            "command": "python",
            "args": ["-m", "debugpy", "--listen", "5678", "--wait-for-client", "${file}"],
            "type": "process",
            "isBackground": true
        }
    ]
}
