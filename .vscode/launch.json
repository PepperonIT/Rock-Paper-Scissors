{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Debug File",
            "type": "python",
            "request": "attach",
            "connect": {
                "host": "localhost",
                "port": 5678
            },
            "pathMappings": [
                {
                    "localRoot": "${workspaceFolder}",
                    "remoteRoot": "."
                }
            ],
            // Remove line if you only want to attach and skip launching the selected python file
            "preLaunchTask": "Launch"
        }
    ]
}
